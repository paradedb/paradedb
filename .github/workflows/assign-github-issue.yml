# workflows/assign-github-issue.yml
#
# Assign GitHub Issue
# Automatically assign an issue to the commenter if they use the '/take' command.

name: Assign GitHub Issue

on:
  issue_comment:
    types: [created]

# Required to assign the issue to the commenter
permissions:
  issues: write

concurrency:
  group: assign-github-issue-${{ github.workflow }}-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  assign-github-issue:
    name: Assign GitHub Issue to Commenter
    runs-on: ubuntu-latest
    if: |
      !github.event.issue.pull_request &&
      contains(github.event.comment.body, '/take')

    steps:
      - name: Check if Commenter Can Be Assigned
        id: check_assignee
        env:
          SECRETS_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_ISSUE_NUMBER: ${{ github.event.issue.number }}
          EVENT_COMMENT_USER_LOGIN: ${{ github.event.comment.user.login }}
        run: |
          HTTP_CODE=$(curl -X GET \
            -H "Authorization: token ${SECRETS_GITHUB_TOKEN}" \
            -H "Accept: application/vnd.github.v3+json" \
            -o /dev/null -w '%{http_code}\n' -s \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/issues/${EVENT_ISSUE_NUMBER}/assignees/${EVENT_COMMENT_USER_LOGIN}")

          if [ "$HTTP_CODE" -eq "204" ]; then
            echo "can_assign=true" >> $GITHUB_OUTPUT
          else
            echo "can_assign=false" >> $GITHUB_OUTPUT
          fi

      - name: Assign GitHub Issue
        env:
          COMMENTER: ${{ github.event.comment.user.login }}
          SECRETS_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_ISSUE_NUMBER: ${{ github.event.issue.number }}
        if: steps.check_assignee.outputs.can_assign == 'true'
        run: |
          curl -X POST \
            -H "Authorization: token ${SECRETS_GITHUB_TOKEN}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d "$(jq -cn --arg commenter "${COMMENTER}" '{assignees: [$commenter]}')" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/issues/${EVENT_ISSUE_NUMBER}/assignees"

          echo "Issue #${EVENT_ISSUE_NUMBER} assigned to ${COMMENTER}"

      - name: Notify of Assignment Failure
        if: steps.check_assignee.outputs.can_assign == 'false'
        uses: actions/github-script@v9
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.name,
              body: `@${context.payload.comment.user.login} Unable to assign this issue to you. You may not have the necessary permissions.`
            })
