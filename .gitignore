# IDEs
.vscode/
.idea/
*.iml
*.swp

# macOS
.DS_Store

# Cargo
target/

# Credentials
# This used to ignore *.json wholesale, which silently swallowed every JSON file
# added to the repo: docs/docs.json needed a negation and context7.json had to be
# force-added. Credentials are matched by name instead, and the detect-private-key
# and detect-aws-credentials pre-commit hooks are the real backstop.
*.pem
*credentials*.json
*service-account*.json
*-key.json

# Code coverage
*.profraw

# Property test regression state
*.proptest-regressions

# Binaries
*.deb

# Ruff
.ruff_cache/

# Env files
.env

# Samply
profile.json*

# Patch/diff
*.orig
*.rej

# Node
# The docs are previewed with a global mintlify install (see docs/README.md), so
# a package.json here is a local artifact of running npm install, not something
# the repository carries.
node_modules/
package.json
package-lock.json

# Nix artifacts
result*

# Cursor
.cursor/

# Claude
.claude/
CLAUDE.md
CLAUDE.local.md

.github/scripts/verify
__pycache__
