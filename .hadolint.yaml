ignored:
  # Only application and extension packages are pinned; Debian utility and library versions are not.
  - DL3008
  # apt-get dist-clean preserves cryptographic state while removing cached package data.
  - DL3009
  # Avoid an inherited SHELL instruction; RUN commands set their own failure behavior.
  - DL4006
