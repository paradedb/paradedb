[workspace]
resolver = "2"
members = [
    "cargo-paradedb",
    "pg_search",
    "shared",
    "tokenizers",
]
exclude = [
    "pg_lakehouse",
]

[profile.dev]
panic = "unwind"

[profile.release]
lto = "fat"
panic = "unwind"
opt-level = 3
codegen-units = 1
