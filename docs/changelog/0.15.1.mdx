---
title: 0.15.1
noindex: true
---

## Stability Improvements 💪

- Fixed a bug accidentally introduced in `0.15.0` where `CREATE INDEX` could deadlock
- Fixed a bug where `paradedb.snippet` does not work over string arrays like `varchar[]` or `text[]`

## Full Changelog

The full changelog is available [on the GitHub Release](https://github.com/paradedb/paradedb/releases/tag/v0.15.1).
