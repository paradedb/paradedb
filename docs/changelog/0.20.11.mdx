---
title: 0.20.11
noindex: true
---

## Bug Fixes 🐛

- Skip custom scan and planner hooks when `pg_search` is preloaded but not installed in the current database, fixing a `"schema paradedb does not exist"` crash (backport of [#4106](https://github.com/paradedb/paradedb/pull/4106)).

The full changelog is available [on the GitHub Release](https://github.com/paradedb/paradedb/releases/tag/v0.20.11).
