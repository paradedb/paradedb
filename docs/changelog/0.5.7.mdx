---
title: 0.5.7
---

## pg_analytics

### Bug Fixes

- Fixed a bug where mutli-line utility statements were being parsed incorrectly.
- Fixed a bug where changing the Postgres search path would break `SELECT` statements over `parquet` tables.

## pg_search

### Bug Fixes

- Fixed a bug where BM25 indexes break `VACUUM FULL`

## Full Changelog

The full changelog is available [here](https://github.com/paradedb/paradedb/releases/tag/v0.5.7).
