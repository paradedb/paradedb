---
header: features
---

Search queries with multiple filters can now use more than one existing Postgres index to find matching rows faster. ParadeDB applies this optimization automatically when it expects a performance benefit.
