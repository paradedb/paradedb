---
header: features
---

An index with `partition_by` now keeps its partitioned layout without a rebuild: a merge routes segments that no build routed, and a concurrent build routes what it wrote once its scan finishes.
