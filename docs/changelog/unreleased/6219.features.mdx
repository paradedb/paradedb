---
header: features
---

A late-materialized string column is now looked up in two steps: the fast-field read that turns a document address into a term ordinal, and the dictionary read that turns the ordinal into the string. Each step can be placed on its own.
