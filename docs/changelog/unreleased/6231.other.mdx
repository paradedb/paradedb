---
header: other
---

A new `paradedb.defer_string_decode` setting pins where the dictionary read of a late-materialized string column runs. It takes `auto` (the default), `on` and `off`, like the existing `paradedb.defer_column_fetch`.
