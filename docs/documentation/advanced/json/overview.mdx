---
title: Overview
---

Use `.` to search over text values nested inside JSON. For instance, the following query searches over a field with values like `{"metadata": {"color": "white"}}`.

```sql
SELECT description, rating, category
FROM mock_items
WHERE id @@@ paradedb.term('metadata.color', 'white');
```

The following query builder functions support JSON fields: [term](/documentation/advanced/term/term), [fuzzy term](/documentation/advanced/term/fuzzy_term), [phrase](/documentation/advanced/phrase/phrase), [fuzzy phrase](/documentation/advanced/phrase/fuzzy_phrase), [phrase prefix](/documentation/advanced/phrase/phrase_prefix), and [range](/documentation/advanced/term/range).
