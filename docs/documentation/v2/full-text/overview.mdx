---
title: Overview
---

## How Text Search Works

Text search in ParadeDB, like most search engines, is centered around the concept of **token matching**.

## Not Substring Matching

While ParadeDB supports substring matching via [regex]() queries, it's important to note that token matching is **not** the
same as substring matching.
