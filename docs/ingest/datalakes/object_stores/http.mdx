---
title: HTTP Server
---

## Overview

This code block demonstrates how to query data stored in common file and open table formats from an arbitrary HTTP server. The file path must start with https://.

```sql
CREATE FOREIGN TABLE parquet_table ()
SERVER parquet_server
OPTIONS (files 'https://<domain>/<path>/<file>.parquet');
```

# TODO: @ming
