---
header: features
---

Vector indexes now route queries to clusters through a stacked IVF index over the centroids instead of a neighborhood graph. For vectors under 256 dimensions the router stops ranking clusters once the estimated recall reaches the new `paradedb.vector_router_recall` GUC (default `0.99`); higher-dimensional vectors, or a target of `1.0`, rank a fixed fraction of the routing index.
