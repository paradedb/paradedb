---
header: features
---

Reduced planning time and eliminated plan non-determinism for the join scan by moving join planning to a single pass at the final query stage. Joins now reliably support `SELECT DISTINCT` with early-termination Top-K ordering and late materialization.
