---
header: features
---

Added a `search_mode` option to the Jieba tokenizer. Set `search_mode=false` to tokenize text without splitting compound words into smaller tokens. The default remains `true`.
