---
header: features
---

Range-partitioned joins are now enabled by default for eligible parallel queries over indexes configured with `partition_by`. To disable this optimization, set `paradedb.enable_range_partitioned_join` to `off`.
